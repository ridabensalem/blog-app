<h1>New Post</h1>

<%= form_with(model: @post, url: user_posts_path(@post.author)) do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.label :text %>
  <%= form.text_area :text %>

  <%= form.submit %>
<% end %>

<%= link_to 'Back', user_posts_path(@post.author) %>
